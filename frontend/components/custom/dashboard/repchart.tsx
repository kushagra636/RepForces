import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function RepChart() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Rep Progress</CardTitle>
      </CardHeader>

      <CardContent>
        <div className="h-40 flex items-center justify-center text-gray-400">
          Chart coming soon 📈
        </div>
      </CardContent>
    </Card>
  );
}
